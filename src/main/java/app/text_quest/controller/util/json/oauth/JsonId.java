package app.text_quest.controller.util.json.oauth;


/**
 * A GSON template for extracting access token from the oauth server answer
 */
public class JsonId {

    /**
     * Id received from the oauth server
     */
    private String id;

    public String getId() {
        return id;
    }
}
